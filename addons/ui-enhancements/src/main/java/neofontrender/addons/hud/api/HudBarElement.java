package neofontrender.addons.hud.api;

/** Identifies the vanilla HUD slot whose layout and cancellation lifecycle hosts a status bar. */
public enum HudBarElement {
    HEALTH,
    ARMOR,
    FOOD,
    AIR,
    MOUNT_HEALTH
}

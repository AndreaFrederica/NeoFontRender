package neofontrender.addons.api.camera;

/** Semantic reason for a camera ray, allowing providers to apply distinct reach policies. */
public enum CameraPickingPurpose { MEASUREMENT, CROSSHAIR, PLAYER_INTERACTION, ITEM_USE, VEHICLE_USE }
